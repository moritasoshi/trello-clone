server:
  servlet:
    context-path: /api/v1

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/trello_clone_db
    username: root
    password: mysql

doma:
  dialect: mysql