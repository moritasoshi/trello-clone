/**
 * path list of trello clone (front)
 */
export const paths = {
  board: "/board/:board_id", // board detail
  board_list: "/", // board list
  sign_in: "/sign-in", // sign in
  sign_up: "/sign-up", // sign up
};

/**
 * base_url for the trello clone api
 */
export const api_base_url = "http://localhost:8080/api/v1";

/**
 * endpoints of trello clone api
 */
